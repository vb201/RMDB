import React from "react";
import { LogoContainer, LogoText } from "./styles";

const Logo = () => {
  return (
    <div>
      <LogoContainer>
        <LogoText>RMDB</LogoText>
      </LogoContainer>
    </div>
  );
};

export default Logo;
